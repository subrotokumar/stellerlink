import 'package:flutter/material.dart';

class RelicsPage extends StatelessWidget {
  const RelicsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Relics Page'),
    );
  }
}
