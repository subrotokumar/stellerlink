import 'package:flutter/material.dart';

class LightConesPage extends StatelessWidget {
  const LightConesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Light Cones'),
    );
  }
}
